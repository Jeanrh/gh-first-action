name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world!"
      - name: Print goodbye
        run: echo "Done - bye!"
      - name: Search cat
        run: |
          curl -X GET "https://api.exemplo.com/facts/random?animal_type=cat&amount=2"
